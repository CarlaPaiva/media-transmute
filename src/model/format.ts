/** Defines a format */
export type Format = {
    /** Name o the format */
    name: string
    /** Extension with a dot */
    extension: string
    /** Mime type of this format, that allow it to be downloaded  */
    mimeType: string
}